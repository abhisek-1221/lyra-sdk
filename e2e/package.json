{
  "name": "@repo/e2e",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "End-to-end tests for the lyra-sdk packages — hits real YouTube APIs, gated on YOUTUBE_API_KEY",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "lyra-sdk": "*"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "vitest": "^2.0.0"
  }
}
